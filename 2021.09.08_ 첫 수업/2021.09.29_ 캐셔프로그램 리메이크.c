#include <stdio.h>

main() {
	
	int m[6] = {5000, 1000, 500, 100, 50, 10}; //배열의 갯수 정할때 0은 처리 안 됨 
	
	int a[4];
	scanf("%d %d %d", &a[1], &a[2], &a[3]);
	a[0] = 10000 - (a[1] + a[2] + a[3]);
	
	printf("\n거스름돈은 %d원 입니다.\n\n", a[0]);
	
	int i;
	for (i=0; i<7; i++) {
		printf("%d원: %d%s\n", m[i], a[0]/m[i], (m[i] >= 1000) ? "장":"개");
		a[0] = a[0] % (m[i]*(a[0]/m[i]));
		
	}
	
}
